# Archive

This directory holds legacy material that is retained for traceability but is not part of the normal app or command-line workflow.

- `backups/` - editor backup files from earlier development.
