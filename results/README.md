# Results Directory

Agatha writes generated periodograms, fitted parameters, phase plots, residuals, and downloaded app data here by default.

Generated result files are ignored by Git. Historical example outputs that were previously tracked live in `examples/results/`.
